# ~/.tmuxinator/insite.yml
# you can make as many tabs as you wish...

name: insite
root: ~/projects/insite
windows:
  - editor: vim
  - console: rails console
  - servers:
      layout: even-horizontal
      panes:
        - docker compose up sqlserver
        - bin/dev
  - logs: tail -f log/development.log
